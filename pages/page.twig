{% extends 'layout.twig' %}

{% block body %}
<div class="row page">
  <div class="col-sm-12">
    <h1 class="section-title">{{ page.title }}</h1>
  </div>

  <div class="col-sm-12">
    <div>{{ page.content | raw }}</div>
  </div>
</div>
{% endblock %}
